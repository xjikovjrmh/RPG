window 窗口 点击animation 选择 animatino 进行动画制作

![image-20260714102711772](C:\Users\Lenovo\AppData\Roaming\Typora\typora-user-images\image-20260714102711772.png)





注意要选中Hierarchy 面板的物体才能进行添加动画

添加idle 闲置待机动画

将 前6个切片拖入动画面板里面， 把第一个切片复制到最后保证流畅

添加行走动画 walking ，同前面的操作

Animator 状态机  选中Player 为角色添加动画逻辑

右击添加过渡（make transition） 左侧参数 make parameters 添加两个float 类型参数 vertical 和 horizontal   负责idle ->walking 的动画切换

反方向 walking 到 idle 的切换 要同时有两个条件， 都小于   &&

![image-20260714104437220](C:\Users\Lenovo\AppData\Roaming\Typora\typora-user-images\image-20260714104437220.png)



接下来让移动脚本能够改变horizontal 和 vertical 的值

```csharp
public Animator anim // 添加动画控制器
    
    anim.SetFloat("horizontal",horizontal);
	anim.SetFloat("vertical",vertical);
    
    
```

最后把player 脚本中的Animator 拖入anim

解决动画延迟问题

- 关闭退出时间（立即切换）， 把duration 设为0

 ![image-20260714105424991](C:\Users\Lenovo\AppData\Roaming\Typora\typora-user-images\image-20260714105424991.png)

解决只有上和有才能播放动画问题， Mathf.abs （传入负数改为正）

解决只有向上下时没有动画疯狂抽动的问题，  从walking -> idle 的两个条件都设置为了 horizontal 



角色翻转，（局部缩放， 修改scale 的x值）



```csharp
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerMovement : MonoBehaviour
{
    public float speed = 5;
    public int facingDirection = 1; //记录面朝方向 1表示向右，-1表示向左

    public Rigidbody2D rb;//处理所有物理效果
    public Animator anim; //动画控制器
                          

    // Update is called once per frame
    void FixedUpdate()  //改成fixedUpdate，每秒50次，保证物理效果的稳定性
    {
        float horizontal = Input.GetAxis("Horizontal");//水平 监听左右 ad 键
        float vertical = Input.GetAxis("Vertical");
        if(horizontal >0 &&transform.localScale.x<0|| horizontal < 0 && transform.localScale.x > 0) //如果水平输入大于0，且面朝方向是向左， -1则翻转 或者水平输入小于0，且面朝方向是向右，1则翻转
        {
            Flip();
        }
        

        anim.SetFloat("horizontal", Mathf.Abs(horizontal));
        anim.SetFloat("vertical", Mathf.Abs(vertical));

        rb.velocity = new Vector2(horizontal,vertical)*speed; //velocity 是速度，Vector2 是一个二维向量，表示物体在 x 和 y 方向上的速度
        //直接改速度，但是保留碰撞，相比transform直接改位置（穿模） 会有更好的物理效果

    }
    void Flip()
    {
        facingDirection *= -1;
        transform.localScale = new Vector3(facingDirection, 1, 1); //要修改整个向量
    }

}

```

