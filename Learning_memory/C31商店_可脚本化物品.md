```csharp
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

[CreateAssetMenu(fileName = "New Item")]

public class ItemSO : ScriptableObject
{
    public string itemName;
    [TextArea] public string itemDescription;//让文本框变大
    public Sprite Icon;

    public bool isGold;
    [Header("Status")]

    public int CurrentHealth;
    public int MaxHealth;
    public int Speed;
    public int Damage;
    [Header("For Temporary Items")]
    public float Duration;


}

```

```csharp
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Loot : MonoBehaviour
{
    public ItemSO itemSO;
    public SpriteRenderer sr;
    public Animator anim;
    public int quantity;//数额

    private void OnValidate()
    {
        if (itemSO == null)
        {
            return;
        }
        sr.sprite = itemSO.Icon;
        this.name = itemSO.itemName;
    }

    private void OnTriggerEnter2D(Collider2D collision)
    {
        if (collision.CompareTag("Player"))
        {
            Debug.Log("Item pickup");
            anim.Play("LootPickup");
            Destroy(gameObject, .5f);
        }
    }

}

```



创建LootPrefab（空物体） ，创建2D物体， Sprite - Circle （Sprite Renderer 在子物体上）  子对象处理视觉表现， 

- 创建动画LootPrefab(可拾取战利品通用动画)，选中父物体， 对子物体排序层级设高 50 让蘑菇显示在玩家上方  第5帧设置回1  向上移动，变形， 第10帧保持位置，变回原来的形状， 第20帧回到原位置，变形，（设置淡出效果，最后一帧alpha设置为0， 第10帧设置为255）只在10-20 之间淡出

逻辑和物理效果由父对象处理：

- 添加circle 2D Collider ，is Trigger





将Mushroom拖入Project 成预制体，把名字改为LootPrefab，将ItemSO留空，后续要加入新的物体，直接create ItemSO ，再拖入











